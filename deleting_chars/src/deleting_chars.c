#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void){

	char str1[100], str2[100];
	int i,l1,l2,j;

	printf("Enter the string: ");
	scanf("%s",str1);

	printf("Enter 2nd one: ");
	scanf("%s",str2);

	l1 = strlen(str1);
	l2 = strlen(str2);

	j=0;


	for (i=0; i<l1; i++)
	{
		if (str1[i] == str2[j])
			{
				for (j=i; j < l2; j++)
				{
					str[]
				}
			}
	}



	return EXIT_SUCCESS;
}
